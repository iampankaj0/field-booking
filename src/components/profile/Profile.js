import React from "react";
import TopHeader from "../../reusableComponents/topheader/TopHeader";

const Profile = () => {
  return (
    <div>
      <TopHeader page_name="Profile" />
    </div>
  );
};

export default Profile;
