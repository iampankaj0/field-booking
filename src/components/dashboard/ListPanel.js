import React from "react";
import styled from "styled-components";

const RightList = styled.div({});

const ListPanel = () => {
  return <RightList>ListPanel</RightList>;
};

export default ListPanel;
